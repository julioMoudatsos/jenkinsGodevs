pipeline {
 agent any
  stages {
 
      stage('Tests For Devs in Godevs Application SanityTest') {
       steps {
         sh "'pwd'"
         sh "'mvn test -Dtest="DevTest"'"
     
       }
   }
   
 }
}
